<UserControl x:Class="GeneratorApp.Views.KeyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GeneratorApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="white">
        <GroupBox Header="{DynamicResource key}" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="CSV: " Margin="10,5,5,5"/>
                <TextBox Text="{Binding CsvKey, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="20" Margin="0,5,10,5"/>
                <TextBlock Text="LDAP: " Margin="10,5,5,5"/>
                <TextBox Text="{Binding LdapKey, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="20" Margin="0,5,10,5"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>
